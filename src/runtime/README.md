# Runtime

Operation execution, transport selection, limits, response handling, and
confirmation policy are implemented in the runtime modules in this directory.

The public behavior is defined by the [MCP compatibility contract](../../docs/mcp-compatibility-contract.md)
and the [MCP integration guide](../../docs/mcp-client-integration.md).
